{
  "name": "projetos-orquestration",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "verify": "turbo run lint typecheck test build"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@11.4.0"
}
