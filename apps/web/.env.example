# Copy this file to .env.local and fill in real values. Never commit .env.local.

# Postgres connection string (Neon in production/preview, local Postgres in dev).
# See docs/decisions/0001-tech-stack.md for the database decision.
DATABASE_URL="postgresql://user:password@localhost:5432/app?schema=public"
